# Personal access token with repo scope — used to register self-hosted runners
GITHUB_RUNNER_PAT=ghp_REPLACE_ME

# Docker socket GID on the host — find with: stat -c '%g' /var/run/docker.sock
DOCKER_GID=999
