POSTGRES_USER=postgres
POSTGRES_PASSWORD=<postgres-root-password>
POSTGRES_DB=postgres

# Passed to init.sh to create per-app users — must match connection strings in identity.env and platform.env
TESTIFY_IDENTITY_DB_PASSWORD=<identity-db-password>
TESTIFY_PLATFORM_DB_PASSWORD=<platform-db-password>
